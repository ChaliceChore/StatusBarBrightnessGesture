<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Guards ACTION_PREFS_CHANGED. Signature level, so only this app (or one
         signed with the same key) can broadcast prefs updates to the hook. -->
    <permission
        android:name="dev.module.statusbarbrightnessgesture.permission.PREFS_CHANGED"
        android:protectionLevel="signature" />
    <uses-permission
        android:name="dev.module.statusbarbrightnessgesture.permission.PREFS_CHANGED" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:supportsRtl="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:theme="@style/Theme.StatusBarBrightnessGesture">

        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="@string/module_description" />
        <meta-data android:name="xposedminversion" android:value="82" />
        <meta-data android:name="xposedscope" android:value="com.android.systemui" />

        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>